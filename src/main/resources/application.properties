spring.application.name=demo
server.port=9001
server.forward-headers-strategy=framework 

# MySQL Configuration
spring.datasource.url=jdbc:mysql://localhost:3306/Data?createDatabaseIfNotExist=true&useSSL=false&serverTimezone=UTC
spring.datasource.username=root
spring.datasource.password=Amypo
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# JPA Configuration
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true
spring.jpa.database-platform=org.hibernate.dialect.MySQL8Dialect

# JWT Configuration
jwt.secret=this_is_a_test_secret_key_must_be_long_enough_for_hmac_sha_which_is_long
jwt.expiration=3600000

# Disable Spring Security for testing (if needed)
# spring.autoconfigure.exclude=org.springframework.boot.autoconfigure.security.servlet.SecurityAutoConfiguration